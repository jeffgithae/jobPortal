services:
  mongodb:
    image: mongo:7
    container_name: job-portal-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: job-portal
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
